function init() {
  const value = 'some string'
  const value2 = "some string"
  const value3 = `fromat ${value}`
  let sum
  sum = null
  sum = 4 + 5 + 9 / 3 * 123

  const obj = {
    value: 'asdlfkj',
    something: value3
  }

  let v = [
    'sdf', obj, value, f2(value)
  ]
}

function square(n) {
  return n * n;
}

const f2 = (n) => {
  return null
}

const actual = square(6)

if (value === 3) {
  f2(3)
} else if (value !== 4) {
  square(value)
} else {
  console.log(value)
}